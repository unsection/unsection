I will create a new template-based category page at `/category/[slug]` and update the navigation to link to these new pages.

### 1. Define Category Constants

Create a new file `app/constants/categories.ts` to manage the mapping between category names and slugs.

* **Export** **`CATEGORY_SLUGS`**: A dictionary mapping category names (e.g., "Hero") to slugs (e.g., "hero-section-design").

* **Export** **`getCategorySlug`**: A helper to safely get slugs.

### 2. Create Category Page Template

Create `app/category/[slug]/page.tsx` to handle dynamic category routes.

* **Duplicate Homepage Layout**: Re-use `TopBar`, `HeroSection`, and `SectionCollections` components.

* **Structure**:

  ```tsx
  export default function CategoryPage({ params }: { params: { slug: string } }) {
    return (
      <main className="min-h-screen bg-black">
        <TopBar />
        <HeroSection />
        <SectionCollections />
      </main>
    );
  }
  ```

  *(Note: This creates the page structure as requested. Content filtering will be added in future iterations.)*

### 3. Update Navigation Components

Update existing navigation components to use `next/link` for internal routing instead of static buttons.

* **`app/components/hero/SideMenu.tsx`**:

  * Import `Link` from `next/link`.

  * Import `CATEGORY_SLUGS`.

  * Replace `<button>` with `<Link href={`/category/${slug}`}>`.

* **`app/components/topbar/MobileMenuOverlay.tsx`**:

  * Import `Link` from `next/link`.

  * Import `CATEGORY_SLUGS`.

  * Replace `<button>` for side menu items with `<Link href={`/category/${slug}`}>`.

### 4. Verification

* Verify the new files are created correctly.

* Verify that navigation links point to the correct `/category/...` URLs.

