I will implement the requested changes by converting the Website/Community buttons into a functional tab interface that filters the collection items, and adding the visual divider.

### 1. Update Data Model and Service
*   **Modify `app/types/index.ts`**: Add a `source` field (`'website' | 'community'`) to the `CollectionItem` interface.
*   **Modify `app/services/collectionService.ts`**:
    *   Update the mock data generation to assign a `source` to each item.
    *   Update `fetchCollections` to accept a `source` parameter and filter the returned data.

### 2. Implement Data Filtering in `SectionCollections`
*   **Modify `app/components/SectionCollections.tsx`**:
    *   Use `useSearchParams` to get the current tab state (defaulting to 'website').
    *   Update the data fetching logic to pass the selected `source` to the service.
    *   Reset the list and pagination when the tab changes.

### 3. Update `Tags` Component UI & Logic
*   **Modify `app/components/hero/Tags.tsx`**:
    *   Replace local state with URL-based state using `useSearchParams` and `useRouter`.
    *   **Tab Interface**:
        *   Implement "Website" and "Community" as mutually exclusive tabs.
        *   **Active Style**: White background, dark text (e.g., for "Website" by default).
        *   **Inactive Style**: Subdued outline style.
    *   **Visual Divider**: Add a `div` with `w-px h-full bg-white/10` between the tabs and the dropdown.
    *   **Interaction**: Clicking a tab updates the URL query parameter (e.g., `?source=community`), triggering the filter in `SectionCollections`.

This approach ensures the UI state is preserved (even on refresh) and the data filtering works seamlessly.
